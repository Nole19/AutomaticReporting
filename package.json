{
  "name": "node_trace_deprecation",
  "description": "",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "deploy": "statusfy build && gh-pages -d dist",
    "test": "echo \"Error: no test specified\" && exit 1",
    "dev": "statusfy dev",
    "build": "statusfy build",
    "start": "statusfy start",
    "generate": "statusfy generate",
    "new-incident": "statusfy new-incident",
    "delete-incident": "statusfy delete-incident",
    "update-incident": "statusfy update-incident"
  },
  "dependencies": {
    "statusfy": "^0.4.3"
  },
  "engines": {
    "node": ">=10",
    "npm": ">=5"
  },
  "devDependencies": {
    "gh-pages": "^6.1.1"
  }
}
